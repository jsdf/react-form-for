{
  "name": "react-form-for",
  "version": "0.2.0",
  "description": "A simple form builder for React in the style of Rails' form_for",
  "main": "./lib/index.js",
  "scripts": {
    "test": "npm run prepublish && tap test",
    "prepublish": "jsx --harmony src/ lib/"
  },
  "author": "James Friend <james@jsdf.co>",
  "license": "MIT",
  "devDependencies": {
    "react": "^0.12.0",
    "react-tools": "^0.12.0",
    "react-bootstrap": "^0.12.0",
    "bootstrap": "^3.3.0",
    "tap": "^0.4.13",
    "browserify": "^6.2.0",
    "node-jsx": "^0.12.0"
  },
  "dependencies": {
    "xtend": "^4.0.0",
    "lodash.memoize": "^2.4.1",
    "lodash.omit": "^2.4.1"
  }
}
